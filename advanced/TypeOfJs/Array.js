// 
const arr = ['1','2','3']

// is equal to obj 
const obj = {
    0:'1',
    1:'2',
    2:'3'
}

console.log('arr', Array.isArray(arr)); // arr true
console.log('obj', Array.isArray(obj)); // obj false
